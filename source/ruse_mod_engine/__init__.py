"""
RUSE Mod Engine — core library for reading, patching, and writing RUSE .dat files.

Architecture:
  edata.py    — .dat (edata) container read/write
  ndfbin.py   — NDF binary parse/serialize + object model
  mod_format.py — .rmod JSON format definition
  applier.py  — applies .rmod patches to game data
"""
